{
  "name": "attestify",
  "version": "0.1.0",
  "private": true,
  "description": "Attestify — Hedera-native attestation protocol",
  "scripts": {
    "build": "pnpm run --recursive build",
    "dev": "pnpm run --recursive dev",
    "lint": "pnpm run -r lint",
    "test": "pnpm run -r test",
    "clean": "pnpm run -r clean"
  },
  "keywords": [
    "attestify",
    "attestation",
    "hedera",
    "hcs",
    "hts",
    "hscs",
    "web3"
  ],
  "license": "MIT"
}
